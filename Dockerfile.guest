FROM python:3.12-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY guest ./guest
RUN chmod +x /app/guest/entrypoint.sh
ENTRYPOINT ["/app/guest/entrypoint.sh"]

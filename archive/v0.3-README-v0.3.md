# Akash → dstack Evidence Layer v0.3

Pinned image:

`ghcr.io/31trainman/akash-dstack-evidence-layer@sha256:53fb7790a35520733f3e913d423cd5549c18fb73d149786538d689932a4e8bac`

New in v0.3:
- deterministic canonical workload claim hashing
- JSON Schema for the claim
- resolved-runtime configuration design note
- fail-closed threat model
- Akash SDL pinned to the immutable GHCR digest
- tests proving image/key/nonce changes alter the claim hash

Critical limitation: this version does **not** claim `config_digest` is hardware-attested yet. The remaining systems problem is obtaining the resolved OCI/Kata runtime state from a measured in-guest component and binding it, the workload public key, and a fresh verifier nonce into the attestation/policy chain.

# Prototype lineage

- v0.3 — canonical workload claim + pinned OCI image
- v0.4 — Kata resolved runtime config digest hook
- v0.5 — trusted manifest-digest bridge from guest image pull
- v0.6 — workload commitment → CoCo Attestation Agent evidence design
- v0.7 — verifier challenge/replay state machine
- v0.8 — Trustee-backed real SEV-SNP adapter
- v0.9 — unified workload verifier + adversarial suite
- v1.0 — real SNP adapter connected to unified workload binding
- v1.1 — consolidated integrated working tree

All future changes should build on v1.1 rather than creating disconnected prototypes.
